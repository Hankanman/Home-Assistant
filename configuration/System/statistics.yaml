<<<<<<< HEAD
---
#----------------------------------#
# Statistics Sensors
#----------------------------------#
statistics:
  sensor:
    #----------------------------------#
    # Bathroom
    #----------------------------------#
    - platform: history_stats
      name: Bathroom Motion Ratio
      entity_id: binary_sensor.bathroom_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bathroom Motion 1 Ratio
      entity_id: binary_sensor.bathroom_motion_1_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bathroom Motion 2 Ratio
      entity_id: binary_sensor.bathroom_motion_2_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bathroom Occupied Ratio
      entity_id: binary_sensor.bathroom_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bathroom Shower Occupied Ratio
      entity_id: input_boolean.shower_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bathroom Humid Ratio
      entity_id: binary_sensor.bathroom_humid
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bathroom Door Ratio
      entity_id: binary_sensor.bathroom_door_contact
      state: "off"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bathroom Schedule Ratio
      entity_id: input_boolean.bathroom_schedule
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bathroom Heating Ratio
      entity_id: climate.bathroom_radiator
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Bedroom
    #----------------------------------#
    - platform: history_stats
      name: Bedroom Motion Ratio
      entity_id: binary_sensor.bedroom_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bedroom Occupied Ratio
      entity_id: binary_sensor.bedroom_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bedroom TV State Ratio
      entity_id: binary_sensor.bedroom_tv_state
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bedroom Motion 1 Ratio
      entity_id: binary_sensor.bedroom_temperature_motion_lux_1_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bedroom Wardrobe Motion 1 Ratio
      entity_id: binary_sensor.wardrobe_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bedroom TV Playing Ratio
      entity_id: media_player.bedroom_tv
      state: "playing"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bedroom Door Ratio
      entity_id: binary_sensor.bedroom_door_contact
      state: "off"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bedroom Dark Ratio
      entity_id: binary_sensor.bedroom_bright
      state: "off"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bedroom Window Ratio
      entity_id: binary_sensor.bedroom_windows
      state: "off"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Bedroom Heating Ratio
      entity_id: climate.bedroom_radiator
      state: "heat"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Entrance
    #----------------------------------#
    - platform: history_stats
      name: Entrance Motion Ratio
      entity_id: binary_sensor.entrance_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Entrance Motion 1 Ratio
      entity_id: binary_sensor.entrance_motion_1_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Entrance Occupied Ratio
      entity_id: binary_sensor.entrance_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Entrance Bright Ratio
      entity_id: binary_sensor.entrance_bright
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Entrance Person Occupancy Ratio
      entity_id: binary_sensor.entrance_person_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Front Drive
    #----------------------------------#
    - platform: history_stats
      name: Front Drive Motion Ratio
      entity_id: binary_sensor.front_drive_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Front Door Motion Ratio
      entity_id: binary_sensor.front_door_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Front Drive Occupied Ratio
      entity_id: binary_sensor.front_drive_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Front Door Ratio
      entity_id: binary_sensor.front_door_contact
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Front Doorbell Ratio
      entity_id: binary_sensor.front_door_ding
      state: "off"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Garden
    #----------------------------------#
    - platform: history_stats
      name: Garden Motion Ratio
      entity_id: binary_sensor.garden_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Garden Occupied Ratio
      entity_id: binary_sensor.garden_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Hall
    #----------------------------------#
    - platform: history_stats
      name: Hall Motion Ratio
      entity_id: binary_sensor.hall_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Hall Stairs Motion Ratio
      entity_id: binary_sensor.stairs_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Hall Motion 1 Ratio
      entity_id: binary_sensor.hall_temperature_motion_lux_1_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Hall Motion 2 Ratio
      entity_id: binary_sensor.hall_motion_1_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Hall Motion 3 Ratio
      entity_id: binary_sensor.hall_motion_2_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Hall Occupied Ratio
      entity_id: binary_sensor.hall_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Hall Person Occupancy Ratio
      entity_id: binary_sensor.hall_person_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Hall Stairs Person Occupancy Ratio
      entity_id: binary_sensor.stairs_person_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Hall Bright Ratio
      entity_id: binary_sensor.hall_bright
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Home
    #----------------------------------#
    - platform: history_stats
      name: Home Motion Ratio
      entity_id: binary_sensor.home_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Home Occupied Ratio
      entity_id: binary_sensor.home_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Kitchen
    #----------------------------------#
    - platform: history_stats
      name: Kitchen Motion Ratio
      entity_id: binary_sensor.kitchen_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Kitchen Occupied Ratio
      entity_id: binary_sensor.kitchen_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Kitchen Bright Ratio
      entity_id: binary_sensor.kitchen_bright
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Kitchen Person Occupancy Ratio
      entity_id: binary_sensor.kitchen_person_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Kitchen Motion 1 Ratio
      entity_id: binary_sensor.kitchen_temperature_motion_lux_1_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Kitchen Motion 2 Ratio
      entity_id: binary_sensor.kitchen_motion_1_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Kitchen Oven Ratio
      entity_id: binary_sensor.kitchen_oven
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Kitchen Hob Ratio
      entity_id: binary_sensor.kitchen_hob
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Kitchen Kettle Ratio
      entity_id: binary_sensor.kitchen_kettle
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Kitchen Microwave Ratio
      entity_id: binary_sensor.kitchen_microwave
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Lounge
    #----------------------------------#
    - platform: history_stats
      name: Lounge Motion Ratio
      entity_id: binary_sensor.lounge_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge Occupied Ratio
      entity_id: binary_sensor.lounge_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge Doors Ratio
      entity_id: binary_sensor.lounge_doors
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge Bright Ratio
      entity_id: binary_sensor.lounge_bright
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge Motion 1 Ratio
      entity_id: binary_sensor.lounge_temperature_motion_lux_1_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge Motion 2 Ratio
      entity_id: binary_sensor.lounge_motion_1_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge Person Occupancy Ratio
      entity_id: binary_sensor.lounge_person_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge Right Door Ratio
      entity_id: binary_sensor.back_door_right_contact
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge Left Door Ratio
      entity_id: binary_sensor.back_door_left_contact
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge Schedule Ratio
      entity_id: input_boolean.lounge_schedule
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge TV On Ratio
      entity_id: binary_sensor.lounge_tv_state
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Lounge TV Playing Ratio
      entity_id: media_player.lounge_tv
      state: "playing"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Outside
    #----------------------------------#
    - platform: history_stats
      name: Outside Motion Ratio
      entity_id: binary_sensor.outside_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Outside Occupied Ratio
      entity_id: binary_sensor.outside_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Shed
    #----------------------------------#


    #----------------------------------#
    # Stairs
    #----------------------------------#
    - platform: history_stats
      name: Stairs Motion Ratio
      entity_id: binary_sensor.stairs_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Stairs Occupied Ratio
      entity_id: binary_sensor.stairs_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Study
    #----------------------------------#
    - platform: history_stats
      name: Study Motion Ratio
      entity_id: binary_sensor.study_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Study Occupied Ratio
      entity_id: binary_sensor.study_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Study Motion 1 Ratio
      entity_id: binary_sensor.study_motion_1_occupancy
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Study Bright Ratio
      entity_id: binary_sensor.study_bright
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Study Door Ratio
      entity_id: binary_sensor.study_door
      state: "off"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Study Starrunner Active Ratio
      entity_id: binary_sensor.starrunner_active
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Study Starrunner Microphone Active Ratio
      entity_id: binary_sensor.starrunner_microphoneactive
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Study Starrunner Webcam Active Ratio
      entity_id: binary_sensor.starrunner_webcamactive
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Study Right Window Ratio
      entity_id: binary_sensor.study_window_right_contact
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Study Left Window Ratio
      entity_id: binary_sensor.study_window_left_contact
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Toilet
    #----------------------------------#
    - platform: history_stats
      name: Toilet Motion Ratio
      entity_id: binary_sensor.toilet_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Toilet Occupied Ratio
      entity_id: binary_sensor.toilet_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Toilet Heating Ratio
      entity_id: climate.toilet_radiator
      state: "heat"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Toilet Bright Ratio
      entity_id: binary_sensor.toilet_bright
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90

    #----------------------------------#
    # Wardrobe
    #----------------------------------#
    - platform: history_stats
      name: Wardrobe Motion Ratio
      entity_id: binary_sensor.wardrobe_motion
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
    - platform: history_stats
      name: Wardrobe Occupied Ratio
      entity_id: binary_sensor.wardrobe_occupied
      state: "on"
      type: time
      end: "{{ now() }}"
      duration:
        days: 90
=======
####################################
# Statistics Sensors
####################################
statistics:
  sensor:
    # Bathroom

    ## Area Entities
    - platform: history_stats
      name: Stats - Bathroom Humidity Trend
      entity_id: binary_sensor.area_bathroom_humidity_trend
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bathroom Humid
      entity_id: binary_sensor.area_bathroom_humid
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bathroom Occupied
      entity_id: binary_sensor.area_bathroom_occupied
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bathroom Temperature Trend
      entity_id: binary_sensor.area_bathroom_temperature_trend
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bathroom Motion
      entity_id: group.area_bathroom_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bathroom Schedule
      entity_id: input_boolean.area_bathroom_schedule
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7

    # Bedroom
    - platform: history_stats
      name: Stats - Bedroom Bright
      entity_id: binary_sensor.area_bedroom_bright
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bedroom Occupied
      entity_id: binary_sensor.area_bedroom_occupied
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bedroom Temperature Trend
      entity_id: binary_sensor.area_bedroom_temperature_trend
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bedroom Motion
      entity_id: group.area_bedroom_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bedroom Windows
      entity_id: group.area_bedroom_windows
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bedroom Schedule
      entity_id: input_boolean.area_bedroom_schedule
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bedroom TV
      entity_id: media_player.bedroom_tv
      state: "playing"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Bedroom Echo
      entity_id: media_player.bedroom_echo
      state: "playing"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7

    # Entrance
    - platform: history_stats
      name: Stats - Entrance Bright
      entity_id: binary_sensor.area_entrance_bright
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Entrance Occupied
      entity_id: binary_sensor.area_entrance_occupied
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Entrance Temperature Trend
      entity_id: binary_sensor.area_entrance_temperature_trend
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Entrance Motion
      entity_id: group.area_entrance_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Entrance Door
      entity_id: binary_sensor.entrance_dor_03_contact
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Entrance Person Motion
      entity_id: binary_sensor.zone_entrance_person_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Entrance Stairs Person Motion
      entity_id: binary_sensor.zone_stairs_person_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7

    # Hall
    - platform: history_stats
      name: Stats - Hall Bright
      entity_id: binary_sensor.area_hall_bright
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Hall Occupied
      entity_id: binary_sensor.area_hall_occupied
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Hall Temperature Trend
      entity_id: binary_sensor.area_hall_temperature_trend
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Hall Motion
      entity_id: group.area_hall_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Hall Person Motion
      entity_id: binary_sensor.zone_hall_person_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7

    # Kitchen
    - platform: history_stats
      name: Stats - Kitchen Bright
      entity_id: binary_sensor.area_kitchen_bright
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Kitchen Occupied
      entity_id: binary_sensor.area_kitchen_occupied
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Kitchen Temperature Trend
      entity_id: binary_sensor.area_kitchen_temperature_trend
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Kitchen Humidity Trend
      entity_id: binary_sensor.area_kitchen_humidity_trend
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Kitchen Motion
      entity_id: group.area_kitchen_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Kitchen Person Motion
      entity_id: binary_sensor.zone_kitchen_person_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Kitchen Schedule
      entity_id: input_boolean.area_kitchen_schedule
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Kitchen Echo Show
      entity_id: media_player.kitchen_echo_show
      state: "playing"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7

    # Lounge
    - platform: history_stats
      name: Stats - Lounge Bright
      entity_id: binary_sensor.area_lounge_bright
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Lounge Occupied
      entity_id: binary_sensor.area_lounge_occupied
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Lounge Temperature Trend
      entity_id: binary_sensor.area_lounge_temperature_trend
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Lounge Motion
      entity_id: group.area_lounge_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Lounge Person Motion
      entity_id: binary_sensor.zone_lounge_person_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Lounge Doors
      entity_id: group.area_lounge_doors
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Lounge Schedule
      entity_id: input_boolean.area_lounge_schedule
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Lounge TV
      entity_id: media_player.lounge_tv
      state: "playing"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7

    # Study
    - platform: history_stats
      name: Stats - Study Bright
      entity_id: binary_sensor.area_study_bright
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Study Occupied
      entity_id: binary_sensor.area_study_occupied
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Study Temperature Trend
      entity_id: binary_sensor.area_study_temperature_trend
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Study Motion
      entity_id: group.area_study_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Study Windows
      entity_id: group.area_study_windows
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Study Schedule
      entity_id: input_boolean.area_study_schedule
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7

    ## Device Entities
    - platform: history_stats
      name: Stats - Starrunner Online
      entity_id: binary_sensor.starrunner_online
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Starrunner Active
      entity_id: binary_sensor.starrunner_active
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Starrunner Webcam Active
      entity_id: binary_sensor.starrunner_webcam_active
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Starrunner Microphone Active
      entity_id: binary_sensor.starrunner_microphone_active
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7

    # Toilet
    - platform: history_stats
      name: Stats - Toilet Bright
      entity_id: binary_sensor.area_toilet_bright
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Toilet Occupied
      entity_id: binary_sensor.area_toilet_occupied
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Toilet Temperature Trend
      entity_id: binary_sensor.area_toilet_temperature_trend
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Toilet Motion
      entity_id: group.area_toilet_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
    - platform: history_stats
      name: Stats - Toilet Windows
      entity_id: group.area_toilet_windows
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7

    # Wardbrobe
    - platform: history_stats
      name: Stats - Wardrobe Motion
      entity_id: group.area_wardrobe_motion
      state: "on"
      type: ratio
      end: "{{ now() }}"
      duration:
        days: 7
>>>>>>> fcf4702d6d9777c62f997420e234fb70032d0604
