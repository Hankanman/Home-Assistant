####################################
# Probabilities
####################################
- platform: template
  sensors:
    bathroom_occupied_probability:
      unit_of_measurement: "%"
      value_template: "{{ (state_attr('binary_sensor.bathroom_occupied','probability')*100 | int) | round(0) }}"
    study_occupied_probability:
      unit_of_measurement: "%"
      value_template: "{{ (state_attr('binary_sensor.study_occupied','probability')*100 | int) | round(0) }}"
    lounge_occupied_probability:
      unit_of_measurement: "%"
      value_template: "{{ (state_attr('binary_sensor.lounge_occupied','probability')*100 | int) | round(0) }}"
    hall_occupied_probability:
      unit_of_measurement: "%"
      value_template: "{{ (state_attr('binary_sensor.hall_occupied','probability')*100 | int) | round(0) }}"
    toilet_occupied_probability:
      unit_of_measurement: "%"
      value_template: "{{ (state_attr('binary_sensor.toilet_occupied','probability')*100 | int) | round(0) }}"
    kitchen_occupied_probability:
      unit_of_measurement: "%"
      value_template: "{{ (state_attr('binary_sensor.kitchen_occupied','probability')*100 | int) | round(0) }}"
    bedroom_occupied_probability:
      unit_of_measurement: "%"
      value_template: "{{ (state_attr('binary_sensor.bedroom_occupied','probability')*100 | int) | round(0) }}"
    entrance_occupied_probability:
      unit_of_measurement: "%"
      value_template: "{{ (state_attr('binary_sensor.entrance_occupied','probability')*100 | int) | round(0) }}"
    in_bed_probability:
      unit_of_measurement: "%"
      value_template: "{{ (state_attr('binary_sensor.in_bed','probability')*100 | int) | round(0) }}"